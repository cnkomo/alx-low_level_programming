#include "main.h"
/**
 * _isupper-evaluate if letter is uppercase
 *@c: print int
 * Return: 0 if successful
 */

int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}

else
{
return (0);
}

}
